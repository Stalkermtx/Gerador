==================================================================================================================
## Instala Server:

```
apt-get update -y; apt-get upgrade -y; wget https://github.com/Cyber-ssh/gerador/raw/main/instala_server; chmod +x instala_server && ./instala_server
```

==================================================================================================================
